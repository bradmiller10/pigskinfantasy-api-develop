export enum DraftType {
  /**
   * Runs a draft starting from the lowest number to the highest number, then reverses and goes back
   * down to the lowest number and repeats until all of the picks are done
   *
   * @author jordanskomer
   * @since 0.0.1
   */
  SnakeCase,
}
